# =============================================================================
# ConfigForge Frontend Environment Variables
# Copy to .env.local for local dev, or set via Vercel dashboard
# =============================================================================

NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_APP_ID=
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
